<%= stylesheet_link_tag 'signin'%>

<div class="container">
	<form class="form-signin">
		<h2 class="form-signin-heading" style="padding-top: 70px">Forgot your password?</h2>

			<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  			<%= devise_error_messages! %>

  			<div class="field" style="padding-bottom: 10px">
    			<%= f.label :email, :class => "sr-only" %><br />
    			<%= f.email_field :email, :class => "form-control", :placeholder => "Email address", autofocus: true %>
  			</div>

  			<div class="actions">
    			<%= f.submit "Send me reset password instructions", :class => "btn btn-lg btn-primary btn-block" %>
  			</div>
			<% end %>
	</form>
</div>

	<div class="container marketing">
      <hr class="featurette-divider" style="padding: 0px">
      <footer>
        <p class="pull-right"><a href="#">Back to top</a></p>
        <p>&copy; 2014 Company, Inc. &middot; <a href="#">Privacy</a> &middot; <a href="#">Terms</a></p>
      </footer>
	</div>


